dùng lệnh này để biên dịch chạy thử trong main

g++ -std=c++23 main.cpp ./Entity/Account/*.cpp ./Entity/Transaction/*.cpp ./Entity/Notification/*.cpp ./UI/*.cpp -o app

để chạy hiện thị trên màn hình console thì dùng: 
 start app.exe